shipkit {
    gitHub.repository = "magneticflux-/java-mumble-link"

    gitHub.readOnlyAuthToken = "0036558481e1d3e998659374a3942d9186b7ca6b"

    gitHub.writeAuthToken = System.getenv("GH_WRITE_TOKEN")
}

allprojects {
    plugins.withId("org.shipkit.bintray") {
        bintray {
            user = 'magneticflux'
            key = System.getenv("BINTRAY_API_KEY")

            pkg {
                repo = 'maven'
                name = 'java-mumble-link'
                licenses = ['LGPL-3.0']
                labels = ['jvm', 'native', 'shared-memory', 'jna', 'mumble', 'mumblelink', 'java']
                vcsUrl = 'https://github.com/magneticflux-/java-mumble-link.git'
            }
        }
    }
}
